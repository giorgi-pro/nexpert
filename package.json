{
  "name": "nexpert",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:portal": "yarn workspace portal dev",
    "dev:dashboard": "yarn workspace dashboard dev",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "reset": "rm -rf **/node_modules yarn.lock **/yarn.lock",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.2.5",
    "turbo": "^2.3.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": ["apps/*", "packages/*"],
  "dependencies": {
    "lint-staged": "^15.2.10",
    "moment": "^2.30.1",
    "nanoid": "^5.0.9",
    "@repo/env": "*"
  }
}
